═══════════════════════════════════════════════════════════════
   GPH BACKEND - GOOGLE CLOUD DEPLOYMENT COMMANDS
═══════════════════════════════════════════════════════════════

🎯 STEP 1: UPLOAD CODE TO YOUR VM
──────────────────────────────────────────────────────────────

Option A - Using PowerShell (from Windows):
  cd C:\Users\91902\Documents\GPH-backend-main
  
  # Update these values first:
  $INSTANCE = "your-vm-name"
  $ZONE = "us-central1-a"
  
  # Then upload:
  gcloud compute scp --recurse . ${INSTANCE}:~/gph-backend --zone=$ZONE

Option B - Using Git (on VM):
  # SSH to VM first, then:
  cd ~
  git clone YOUR_GITHUB_REPO_URL gph-backend
  cd gph-backend

═══════════════════════════════════════════════════════════════
🎯 STEP 2: SSH INTO YOUR VM
──────────────────────────────────────────────────────────────

gcloud compute ssh your-vm-name --zone=your-zone

# OR use the "SSH" button in Google Cloud Console

═══════════════════════════════════════════════════════════════
🎯 STEP 3: RUN DEPLOYMENT (ON YOUR VM)
──────────────────────────────────────────────────────────────

cd ~/gph-backend
chmod +x deploy.sh
sudo bash deploy.sh

# This will take 2-3 minutes...
# It installs Python, Nginx, creates user, sets up everything!

═══════════════════════════════════════════════════════════════
🎯 STEP 4: GENERATE SECURE KEYS (ON YOUR VM)
──────────────────────────────────────────────────────────────

python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_urlsafe(32))"
python3 -c "import secrets; print('JWT_SECRET_KEY=' + secrets.token_urlsafe(32))"
python3 -c "import secrets; print('API_ADMIN_KEY=' + secrets.token_urlsafe(32))"

# COPY THESE THREE KEYS! You'll need them in the next step.

═══════════════════════════════════════════════════════════════
🎯 STEP 5: UPDATE CONFIGURATION (ON YOUR VM)
──────────────────────────────────────────────────────────────

sudo nano /opt/gph-backend/.env

# Find and update these three lines with keys from Step 4:
SECRET_KEY=paste-key-from-step-4-here
JWT_SECRET_KEY=paste-key-from-step-4-here
API_ADMIN_KEY=paste-key-from-step-4-here

# Save: Ctrl+X, then Y, then Enter

═══════════════════════════════════════════════════════════════
🎯 STEP 6: RESTART SERVICES (ON YOUR VM)
──────────────────────────────────────────────────────────────

sudo systemctl restart gph-backend
sudo systemctl restart nginx
sudo systemctl status gph-backend

═══════════════════════════════════════════════════════════════
🎯 STEP 7: GET YOUR SERVER IP (ON YOUR VM)
──────────────────────────────────────────────────────────────

curl ifconfig.me

# Write down this IP address! Example: 34.123.45.67

═══════════════════════════════════════════════════════════════
🎯 STEP 8: CONFIGURE FIREWALL IN GOOGLE CLOUD CONSOLE
──────────────────────────────────────────────────────────────

1. Open Google Cloud Console in your browser
2. Go to: VPC Network → Firewall
3. Click "CREATE FIREWALL RULE"
4. Fill in:
   Name: allow-http
   Targets: All instances in the network
   Source IP ranges: 0.0.0.0/0
   Protocols and ports: 
     ☑ tcp: 80
5. Click CREATE

═══════════════════════════════════════════════════════════════
🎯 STEP 9: TEST YOUR API (ON YOUR VM OR LOCAL)
──────────────────────────────────────────────────────────────

# Replace YOUR_IP with the IP from Step 7
curl http://YOUR_IP/api/officers
curl http://YOUR_IP/api/duties

# Should return JSON data!

═══════════════════════════════════════════════════════════════
🎯 STEP 10: UPDATE YOUR FRONTEND
──────────────────────────────────────────────────────────────

Edit: C:\Users\91902\Desktop\vigil-goa-dash\src\config\api.ts

Change:
export const API_BASE_URL = 'http://YOUR_IP';

Replace YOUR_IP with the IP from Step 7

Then rebuild frontend:
cd C:\Users\91902\Desktop\vigil-goa-dash
npm run build

═══════════════════════════════════════════════════════════════
✅ VERIFICATION CHECKLIST
──────────────────────────────────────────────────────────────

□ Code uploaded to VM
□ Deployment script completed successfully
□ .env file updated with secure keys
□ Services running (gph-backend and nginx)
□ Firewall rule created in Google Cloud
□ API responds to external requests
□ Frontend updated with new API URL

═══════════════════════════════════════════════════════════════
📋 USEFUL COMMANDS
──────────────────────────────────────────────────────────────

View logs (live):
  sudo journalctl -u gph-backend -f

View last 50 log lines:
  sudo journalctl -u gph-backend -n 50

Restart application:
  sudo systemctl restart gph-backend

Check status:
  sudo systemctl status gph-backend

View error logs:
  sudo tail -f /var/log/gph-backend/error.log

Edit configuration:
  sudo nano /opt/gph-backend/.env
  sudo systemctl restart gph-backend

═══════════════════════════════════════════════════════════════
🆘 TROUBLESHOOTING
──────────────────────────────────────────────────────────────

Service won't start:
  sudo journalctl -u gph-backend -n 100
  
Can't connect from outside:
  # Check firewall
  sudo ufw status
  # Should show: Status: active, Port 80 ALLOW
  
Database errors:
  # Test connection (use your own credentials)
  mysql -h YOUR_DB_HOST -P YOUR_DB_PORT -u YOUR_DB_USER -p

Permission denied:
  sudo chown -R gph:gph /opt/gph-backend
  sudo systemctl restart gph-backend

═══════════════════════════════════════════════════════════════
🎉 DONE!
──────────────────────────────────────────────────────────────

Your backend is now running at: http://YOUR_IP

Test endpoints:
  http://YOUR_IP/api/officers
  http://YOUR_IP/api/duties
  http://YOUR_IP/api/duty-locations
  
═══════════════════════════════════════════════════════════════
